# Checkpoint Posture

No Narada proper checkpoint history exists in this seed.

The bootstrap initial memory is external orientation evidence with status `external_orientation_pending_admission`. It is not a Narada proper checkpoint, resume state, agent-context database, or runtime memory import.

Do not copy narada-andrey checkpoints or agent-context SQLite databases here as authority.
