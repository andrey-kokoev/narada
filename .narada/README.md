# Narada Proper Site Seed

This directory is the initial Narada proper Site seed substrate.

It is not a mirror of `C:\Users\Andrey\Narada`, not a copy of narada-andrey runtime state, and not a prior Narada proper checkpoint. It exists to give Narada proper a small durable place for startup orientation, admission decisions, capability posture, and future Site-local substrate work.

The first startup path is `.narada/bootstrap/startup.md`.

The first memory object remains external orientation evidence with status `external_orientation_pending_admission`. It is not Narada proper task history, inbox authority, roster authority, runtime state, or checkpoint memory.

This seed was written to `D:\code\narada` only after the operator explicitly admitted that path as the temporary mutation authority for this seed because `/home/andrey/src/narada` was not reachable from the active carrier.
