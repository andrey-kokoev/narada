# Phase A Task — End-to-End Daemon Integration Test (Real Runtime Path)

## Objective

Produce an integration test that proves the full Phase A path using the real `CodexCharterRunner` with a mocked HTTP backend.

## Changes

1. Create `packages/exchange-fs-sync-daemon/test/integration/dispatch-real.test.ts`

## Test Flow

1. Write config with `charter.runtime: "codex-api"` and `api_key: "test-key"`
2. Provide `GraphAdapter` mock that returns one `created` event
3. Mock `global.fetch` to return a `CharterOutputEnvelope` with `send_reply` action
4. Start daemon service
5. Wait for sync + dispatch
6. Stop service
7. Query SQLite coordinator DB and assert:
   - `work_items` row exists and `status === "resolved"`
   - `execution_attempts` row exists and `status === "succeeded"`
   - `outbound_commands` row exists for the conversation

## Acceptance Criteria

- Test passes reliably
- Does not require real OpenAI API key or network access
- `pnpm test` passes in `exchange-fs-sync-daemon`
