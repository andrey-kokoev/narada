# exchange-fs-sync-root

Pnpm monorepo for exchange-fs-sync.

## Structure

- `packages/exchange-fs-sync/` - Main filesystem-backed Exchange sync implementation

## Commands

- `pnpm install` - Install dependencies
- `pnpm test` - Run tests in all packages
- `pnpm typecheck` - Type check all packages
