# Carrier command clients

This directory owns canonical operator-facing command implementations. The
flat `scripts/cloudflare-carrier-*.mjs` files are compatibility entrypoints so
package script names and existing operator invocations remain stable.
