{
  "name": "@narada-core/concepts",
  "version": "0.1.0",
  "description": "ConceptRegistry storage, validation, and lookup for Narada concepts",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "records/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.3.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/domains/concepts"
  },
  "keywords": [
    "narada",
    "concept",
    "concept-registry",
    "registry"
  ],
  "author": "Narada <team@narada.dev>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bunx --bun tsc -p tsconfig.json",
    "build:node": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.json",
    "typecheck:node": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "bunx --bun vitest run",
    "test:node": "pnpm exec vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.10"
  },
  "homepage": "https://github.com/narada-core/narada",
  "bugs": {
    "url": "https://github.com/narada-core/narada/issues"
  }
}
