# @narada2/carrier-runtime-contract

Shared runtime launch, MCP runtime, terminal runtime, heartbeat, and session identity contracts for Narada carrier surfaces.

This package currently owns the launch-slice, MCP-runtime, and terminal-runtime JSON contracts consumed by `agent-tui`. It is the intended home for broader carrier runtime identity and heartbeat schemas as those are extracted.
