{
  "name": "@narada-core/ui",
  "version": "0.1.0",
  "type": "module",
  "description": "Narada renderer-neutral design tokens and compiled UI foundation",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/ui"
  },
  "homepage": "https://github.com/narada-core/narada",
  "bugs": {
    "url": "https://github.com/narada-core/narada/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    "./styles.css": "./dist/styles.css",
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prepublishOnly": "node --import tsx ../../scripts/assert-publication-admission.ts",
    "prepack": "pnpm run build",
    "build": "bunx --bun vite build",
    "build:node": "pnpm exec vite build",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.json",
    "typecheck:node": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "pnpm run build && pnpm run typecheck && bun test test/ui-foundation.test.ts test/consumer-contract.test.ts",
    "test:node": "pnpm run build:node && pnpm run typecheck:node && node --import tsx --test test/ui-foundation.test.ts test/consumer-contract.test.ts"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.1.18",
    "vite": "^8.1.5",
    "typescript": "^5.3.0",
    "tsx": "^4.7.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.3.0"
  }
}
