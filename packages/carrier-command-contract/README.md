# @narada2/carrier-command-contract

Shared carrier command vocabulary and semantic effect contract for Narada carrier surfaces.

Carrier implementations may render command help differently, but command tokens, aliases, arguments, and semantic effects should be fixture-verified against this package.
