/**
 * Deprecated typed-MCP inbox admission shim retired by #415.
 *
 * This tombstone intentionally exports no compatibility surface. Use
 * tools/inbox/admission-log.mjs for canonical inbox admission helpers.
 * The file remains only because the current MCP filesystem surface exposes
 * audited writes but no audited delete operation.
 */
