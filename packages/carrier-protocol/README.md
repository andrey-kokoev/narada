# Carrier Protocol

Shared protocol types and validators for Narada interactive carriers.

This package is the executable contract behind:

- `packages/agent-tui/target-functionality.md`
- `packages/agent-tui/carrier-protocol.md`

It is UI-independent and may be used by both `agent-cli` and `agent-tui`.
