{
  "name": "exchange-fs-sync",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "exchange-fs-sync": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/cli/main.js",
    "sync": "npm run build && node dist/cli/main.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
