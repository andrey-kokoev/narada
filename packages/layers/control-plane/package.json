{
  "name": "@narada-core/control-plane",
  "version": "0.1.0",
  "description": "Deterministic kernel for compiling remote source deltas into local state and durable side-effect intents",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./lifecycle": {
      "import": "./dist/lifecycle/index.js",
      "types": "./dist/lifecycle/index.d.ts"
    }
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    "config.schema.json"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/layers/control-plane"
  },
  "keywords": [
    "narada",
    "state-compiler",
    "sync",
    "exchange",
    "microsoft-graph",
    "email",
    "automation",
    "control-plane"
  ],
  "author": "Narada <team@narada.dev>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublishOnly": "node --import tsx ../../../scripts/assert-publication-admission.ts",
    "build": "tsc && pnpm run generate:config-schema",
    "typecheck": "tsc --noEmit",
    "generate:config-schema": "node --import tsx scripts/generate-config-schema.ts",
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "benchmark": "vitest bench --run",
    "benchmark:watch": "vitest bench",
    "benchmark:report": "vitest bench --run --reporter=verbose",
    "profile": "node --inspect --expose-gc ../scripts/profile.ts"
  },
  "dependencies": {
    "@narada-core/charters": "workspace:*",
    "@narada-core/process-launch-posture": "workspace:*",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^4.1.10",
    "fast-check": "^3.15.0",
    "memfs": "^4.6.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.10",
    "zod-to-json-schema": "^3.25.2"
  },
  "homepage": "https://github.com/narada-core/narada",
  "bugs": {
    "url": "https://github.com/narada-core/narada/issues"
  }
}