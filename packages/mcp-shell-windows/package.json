{
  "name": "@narada-core/mcp-shell-windows",
  "version": "0.1.0",
  "description": "Policy descriptors for audited Windows shell-like MCP execution",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bunx --bun tsc",
    "build:node": "pnpm exec tsc",
    "typecheck": "bunx --bun tsc --noEmit",
    "typecheck:node": "pnpm exec tsc --noEmit",
    "test": "bunx --bun vitest run",
    "test:node": "pnpm exec vitest run"
  },
  "dependencies": {
    "@narada-core/mcp-transport": "workspace:*",
    "@narada-core/process-launch-posture": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.3.0"
  },
  "bin": {
    "narada-shell-mcp": "./server.ts"
  },
  "files": [
    "dist/",
    "server.ts",
    "support/",
    "README.md"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/mcp-shell-windows"
  }
}
