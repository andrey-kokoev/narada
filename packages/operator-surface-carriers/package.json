{
  "name": "@narada2/operator-surface-carriers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm run build && node --import tsx --test src/operator-surface-carriers.test.ts"
  },
  "dependencies": {
    "@narada2/process-launch-posture": "workspace:*"
  },
  "description": "Operator surface carrier templates",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andrey-kokoev/narada.git",
    "directory": "packages/operator-surface-carriers"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "dist/"
  ]
}
