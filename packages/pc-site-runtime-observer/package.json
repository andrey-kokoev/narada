{
  "name": "@narada-core/pc-site-runtime-observer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Native low-overhead PC Site MCP runtime resource observer and evidence store.",
  "bin": { "narada-pc-site-runtime-observer": "./dist/main.js" },
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "node --import tsx scripts/build.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm run build && cargo test --manifest-path native/Cargo.toml && node --import tsx --test test/*.test.ts",
    "test:live:e2e": "pnpm run build && node --import tsx --test test/*.live.e2e.ts"
  },
  "dependencies": { "@narada-core/process-launch-posture": "workspace:*" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.7.0", "typescript": "^5.3.0" },
  "engines": { "node": ">=22.0.0" },
  "files": ["dist/", "native/target/release/narada-pc-site-runtime-observer.exe", "README.md"]
}
