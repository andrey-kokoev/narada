{
  "name": "@narada-core/carrier-protocol-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/carrier-protocol-contract.d.ts",
      "import": "./dist/carrier-protocol-contract.js"
    },
    "./carrier-protocol": "./contracts/carrier-protocol.json"
  },
  "scripts": {
    "build": "bunx --bun tsc -p tsconfig.build.json",
    "build:node": "pnpm exec tsc -p tsconfig.build.json",
    "test": "bun test src/carrier-protocol-contract.test.ts",
    "test:node": "node --import tsx --test src/carrier-protocol-contract.test.ts",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.json",
    "typecheck:node": "pnpm exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@narada-core/carrier-protocol": "workspace:*"
  },
  "description": "Carrier protocol contracts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/carrier-protocol-contract"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.3.0"
  }
}
