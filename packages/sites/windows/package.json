{
  "name": "@narada-core/windows-site",
  "version": "0.1.0",
  "description": "Windows Site materialization for Narada bounded Cycle execution",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "files": [
    "dist/",
    "scripts/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "bunx --bun tsc",
    "build:node": "pnpm exec tsc",
    "typecheck": "bunx --bun tsc --noEmit",
    "typecheck:node": "pnpm exec tsc --noEmit",
    "continuity:windows": "bun scripts/windows-site-continuity-sync.ts",
    "continuity:windows:node": "node --import tsx scripts/windows-site-continuity-sync.ts",
    "test": "bunx --bun vitest run",
    "test:node": "pnpm exec vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@narada-core/control-plane": "workspace:*",
    "@narada-core/site-registry-contract": "workspace:*",
    "@narada-core/site-continuity": "workspace:*",
    "@narada-core/sqlite": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^4.1.10"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narada-core/narada.git",
    "directory": "packages/sites/windows"
  }
}
